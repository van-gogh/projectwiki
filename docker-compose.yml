services:
  projectwiki:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - projectwiki-data:/data
      - ./:/workspace:ro
    environment:
      PROJECTWIKI_DATA_DIR: /data

volumes:
  projectwiki-data:
