services:
  whywiki:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - whywiki-data:/data
      - ./:/workspace:ro
    environment:
      WHYWIKI_DATA_DIR: /data

volumes:
  whywiki-data:
